# Read the key from the environment variable set by Docker Compose
gemini.api.key=${GEMINI_API_KEY}

# Read the endpoint from the environment variable set by Docker Compose
amazon.dynamodb.endpoint=${AMAZON_DYNAMODB_ENDPOINT}

# These are still needed for the AWS SDK to initialize
amazon.aws.accesskey=dummy
amazon.aws.secretkey=dummy
amazon.aws.region=us-east-1
